# Model weights — large third-party downloads, fetched via download-models.sh
*.safetensors
*.ckpt
*.pt
*.pth
*.bin
*.onnx

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# A cloned ComfyUI engine, if anyone clones it inside the repo
/ComfyUI/

# pixel-snapper: Rust source + build (cloned & built by install.sh, see --snap-pixels)
/tools/

# Generated images / scratch
output/
*.tmp

# Personal --server aliases (your machines/IPs). Ship servers.example.json instead.
servers.json

# OS / editor cruft
.DS_Store
*.swp
